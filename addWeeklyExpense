const addWeeklyExpense = async (date, expenseData) => {
  try {
    const weeklyExpenseRef = db.collection('weeklyExpenses').doc(date);
    await weeklyExpenseRef.set(expenseData);
    return true;
  } catch (error) {
    console.error('Error adding weekly expense:', error);
    return false;
  }
};
